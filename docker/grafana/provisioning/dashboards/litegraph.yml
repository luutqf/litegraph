apiVersion: 1

providers:
  - name: LiteGraph
    orgId: 1
    folder: LiteGraph
    type: file
    disableDeletion: false
    updateIntervalSeconds: 30
    allowUiUpdates: true
    options:
      path: /var/lib/grafana/dashboards/litegraph
